createTemplates is a tool through which we can create multiple images automatically reflected on the template.

For example: On the one cup reflected thousands of images, a lot of different cases reflected on one phone.

To run app you have to use a php